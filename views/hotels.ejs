<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HOTELERIA</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container">
          <a class="navbar-brand" href="#">Hotelería</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
              <li class="nav-item">
                <a class="nav-link" href="/hoteles">Listado de Hoteles</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/">Hoteles</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/users">Usuarios</a>
              </li>
            </ul>
          </div>
        </div>
    </nav>  

    <div class="container">
        <h1 class="text-center">Listado de Hoteles</h1>

        <ul class="list-unstyled">
            <% hotels.forEach(hotel => { %>
                <li class="bg-light p-4 rounded mb-4 shadow-sm">
                    <strong>Nombre:</strong> <%= hotel.name %><br>
                    <strong>Dirección:</strong> <%= hotel.address %><br>
                    <strong>Rating:</strong> <%= hotel.rating %><br>
                    <strong>Precio:</strong> <%= hotel.price %><br>
                    
                    <!-- Botón para eliminar el hotel -->
                    <form action="/eliminar-hotel" method="POST" class="d-inline">
                        <input type="hidden" name="hotelId" value="<%= hotel._id %>">
                        <button type="submit" class="btn btn-danger btn-sm">Eliminar</button>
                    </form>
                </li>
            <% }); %>
        </ul>
    </div>
</body>
</html>
